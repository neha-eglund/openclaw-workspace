# Secrets and internal config — never commit
config/slack-tokens.json
config/slack-webhooks.json
config/contributor-names.json

# Runtime artifacts
state/
nightly-results/
.openclaw-cli-images/
.openclaw/
nightly-ai-jobs-slack.md
nightly-ai-jobs-solutions-report.md

# IDE
.idea/

# Nested workspace/worktree directories (each has its own .git)
claude/
claude-github-demo/
fast/
nightly/
powerful/

# Packaged skill bundles (source-of-truth is the skills/<name>/ folders)
*.skill

# Temporary files
/tmp/
*.pdf
*.pyc
__pycache__/

# OS
.DS_Store
